[workspace]
members = ["openvm-guest", "openvm-host"]
resolver = "2"

[workspace.dependencies]
zkvm_programs = { path = "../programs" }

# Optimize for zkVM execution
[profile.release]
opt-level = 3
lto = true
