[workspace]
members = ["pico-guest", "pico-host"]
resolver = "2"

[workspace.dependencies]
fib = { path = "../fib" }
common = { path = "../common" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
