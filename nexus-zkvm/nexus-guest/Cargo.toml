[package]
name = "nexus-guest"
version = "0.1.0"
edition = "2024"

[dependencies]
nexus-rt = { git = "https://github.com/nexus-xyz/nexus-zkvm.git", tag = "0.3.4", version = "0.3.4" }
programs = { path = "../../programs", default-features = false }

# Generated by cargo-nexus-zkvm, do not remove!
#
[features]
cycles = [] # Enable cycle counting for run command
