[package]
name = "valida-guest"
version = "0.1.0"
edition = "2021"

[dependencies]
zkvm_programs = { path = "../../../programs" }

[profile.release]
panic = "abort"
lto = true
opt-level = 3
codegen-units = 1
