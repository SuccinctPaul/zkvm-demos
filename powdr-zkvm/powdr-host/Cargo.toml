[package]
name = "powdr-host"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT OR Apache-2.0"

[dependencies]
common = { workspace = true }
fib = { workspace = true }
dotenv = { workspace = true }
anyhow = { workspace = true }
env_logger = { workspace = true }
log = "0.4"

# Powdr dependencies
# Note: These will be available once Powdr SDK is published
# For now, this is a placeholder implementation
# powdr-backend = { git = "https://github.com/powdr-labs/powdr", branch = "main" }
# powdr-pipeline = { git = "https://github.com/powdr-labs/powdr", branch = "main" }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
# powdr-build = { git = "https://github.com/powdr-labs/powdr", branch = "main" }

