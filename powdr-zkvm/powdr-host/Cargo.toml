[package]
name = "powdr-host"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT OR Apache-2.0"

[dependencies]
zkvm_programs = { workspace = true }
dotenv = { workspace = true }
anyhow = { workspace = true }
env_logger = { workspace = true }
log = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

# Note: Powdr SDK dependencies are used via CLI
# The powdr-rs CLI provides all proving functionality
# Install with: cargo install powdr-cli

[build-dependencies]
