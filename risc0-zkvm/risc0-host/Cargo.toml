[package]
name = "risc0-host"
version = "0.1.0"
edition = "2021"

[dependencies]
methods = { path = "../risc0-guest" }
risc0-zkvm = { version = "^2.1.0", default-features = true, features = ["circuit_debug", "witgen_debug"] }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

common.workspace = true
dotenv.workspace = true
