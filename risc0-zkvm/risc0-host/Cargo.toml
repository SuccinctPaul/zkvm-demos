[package]
name = "risc0-host"
version = "0.1.0"
edition = "2021"

[dependencies]
methods = { path = "../risc0-guest" }
risc0-zkvm = { version = "=3.0.3", default-features = true }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

common = { path = "../../common" }
dotenv = "0.15.0"
