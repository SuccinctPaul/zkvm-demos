[package]
name = "zkwasm-guest"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# No dependencies needed for basic zkWasm guest programs
# zkWasm provides host functions directly via extern "C"

