[package]
name = "zkwasm-guest"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
zkvm_programs = { path = "../../programs", default-features = false }
wee_alloc = "0.4.5"

[features]
default = []
std = []
