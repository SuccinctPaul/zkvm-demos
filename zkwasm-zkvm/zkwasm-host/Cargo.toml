[package]
name = "zkwasm-host"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
clap = { version = "4.5", features = ["derive"] }
programs = { path = "../../programs" }

# Note: zkWasm CLI is typically installed separately and used via command line
# This host program will orchestrate the CLI commands
