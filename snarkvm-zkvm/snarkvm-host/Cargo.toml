[package]
name = "snarkvm-host"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT OR Apache-2.0"

[dependencies]
zkvm_programs.workspace = true
anyhow.workspace = true
colored.workspace = true

# Note: snarkVM SDK functionality is accessed via Leo CLI
# The Leo CLI provides Varuna proof generation and verification
# Install with: cargo install leo-lang
