[package]
name = "novanet-guest"
version = "0.1.0"
edition = "2021"

[dependencies]
programs = { path = "../../programs", default-features = false }
serde = { version = "1.0", features = ["derive"] }
