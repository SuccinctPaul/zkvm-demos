# mise-related files to add to .gitignore

# Local mise configuration (user-specific)
.mise.local.toml

# mise cache and temporary files
.mise/
.mise.lock

# Tool versions installed by mise
.tool-versions.local

