[package]
name = "jolt-guest"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../../common", default-features = false }
jolt = { package = "jolt-sdk", git = "https://github.com/a16z/jolt",  tag = "v0.3.0-alpha" }

[features]
guest = []
